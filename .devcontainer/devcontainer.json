// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/debian
{
  "name": "Debian",
  "build": {
    "dockerfile": "Dockerfile"
  },

  // Features to add to the dev container. More info: https://containers.dev/features.
  // "features": {},

  // Use 'forwardPorts' to make a list of ports inside the container available locally.
  // "forwardPorts": [],

  // Configure tool-specific properties.
  // "customizations": {},

  // Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
  // "remoteUser": "root"

  "initializeCommand": "echo '=== This is initialize command! ==='",
  "onCreateCommand": "echo '=== This is onCreate command! ==='",
  "updateContentCommand": "echo '=== This is updateContent command! ==='",
  "postCreateCommand": "echo '=== This is postCreate command! ==='",
  "postStartCommand": "echo '=== This is postStart command! ==='",
  "postAttachCommand": "echo '=== This is postAttach command! ==='"
}
